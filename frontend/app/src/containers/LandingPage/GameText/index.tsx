import React, { useState, useEffect } from 'react';
import { Socket } from 'socket.io-client';

type 

export const GameText:React.FunctionComponent = (props:{text:string}) => {

    return (
        <div>
            asd
        </div>
    )

}

export default GameText;
